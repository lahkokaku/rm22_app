<div class="form-group mb-4">
    <label for="{{$id}}" class="col-form-label">{{$slot}}</label>
    <input type="{{$type}}" class="form-control rounded-pill" id="{{$id}}" name="{{$name}}" value="{{$value}}" >
</div>